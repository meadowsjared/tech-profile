/**
 * Constants used throughout the application
 */

/** measured in pixels per second */
export const STAR_SPEED = 50;
/** measured in pixels per second */
export const STAR_SPEED_RESET_SPEED = 25;
/**
 * the speed of the background
 *
 * converts movement vector magnitude into pixels per second
 * magnitude of 400 will result in 20 pixels per second
 */
export const BG_SPEED = 2 / 20;
export const BG_SPEED_RESET_SPEED = 10 / 20;
